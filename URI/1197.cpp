# include <bits/stdc++.h>

using namespace std;

# define _ ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

int t, v;

int main () {_
  while (cin >> v >> t) {
    cout << 2 * v * t << "\n";
  }
  
  return 0;
}