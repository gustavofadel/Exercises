# include <bits/stdc++.h>

using namespace std;

# define _ ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

int p, r;

int main () {_
  cin >> p >> r; cout << (p ? r ? "A" : "B" : "C") << "\n";

  return 0;
}