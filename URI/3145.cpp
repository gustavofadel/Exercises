# include <bits/stdc++.h>

using namespace std;

# define _ ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);

int n, x;

int main () {_
  cin >> n >> x; cout << fixed << setprecision(2) << (float) x / (n + 2) << "\n";
  
  return 0;
}